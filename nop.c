#include "monty.h"
/**
 * nop - does not do any thing.
 * @stack: is the doulbe linked list.
 * @line_number: is the line number.
 */
void nop(stack_t **stack, int line_number)
{
	(void)stack;
	(void)line_number;
}
