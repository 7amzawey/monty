#include "monty.h"
/**
 * comment - doesn't do anything
 * @stack: is the stack
 * @line_number: is the line_number
 */
void comment(stack_t **stack, int line_number)
{
	(void)stack;
	(void)line_number;
}
